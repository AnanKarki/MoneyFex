@using PagedList;
@using PagedList.Mvc;
@model IPagedList<FAXER.PORTAL.Areas.Admin.ViewModels.ReceiverDetailsInfoViewModel>
@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/New_Layout.cshtml";
    //string[] Alpha = ViewBag.Alpha;

}


@*<div class="pcoded-content">
        <div class="pcoded-inner-content">

            <div class="main-body">
                <div class="page-wrapper">

                    <div class="page-header">
                        <div class="row align-items-end">
                            <div class="col-lg-8">
                                <div class="page-header-title">
                                    <div class="d-inline">
                                        <h4>Registered Receivers</h4>

                                    </div>
                                </div>
                            </div>
                            <div class="col-lg-4">
                                <div class="page-header-breadcrumb">
                                    <ul class="breadcrumb-title">
                                        <li class="breadcrumb-item">
                                            <a href="index.php"> <i class="feather icon-home"></i> </a>
                                        </li>

                                        <li class="breadcrumb-item">
                                            <a href="#!">Registered Receivers</a>
                                        </li>
                                    </ul>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="page-body">
                        <div class="row">

                            <div class="col-sm-12">

                                <div class="card">

                                    <div class="card-block">
                                        <div class="row ">

                                            <div class="col-xl-4 col-md-6">
                                                <div class="form-group">
                                                    @Html.DropDownList("Country", (IEnumerable<SelectListItem>)ViewBag.Countries, "Filter By Country", new { @class = "form-control form-control-round form-control-bold", id = "Country", onchange = "filter();" })

                                                </div>
                                            </div>


                                            <div class="col-xl-4 col-md-6">
                                                <div class="form-group">
                                                    @Html.DropDownList("City", (IEnumerable<SelectListItem>)ViewBag.Cities, "Choose City", new { @class = "form-control form-control-round form-control-bold", id = "City", onchange = "filter();" })

                                                </div>
                                            </div>


                                            <div class="col-xl-4 col-md-6">
                                                <div class="form-group">

                                                    <input name="" class="form-control form-control-round form-control-bold" id="receiverName" placeholder="Search Receiver" onkeyup="filter();">
                                                </div>

                                            </div>
                                        </div>

                                        <div class="m-b-15">


                                            <button class="btn btn-primary btn-round" data-toggle="modal" data-target="#email">Send Email</button>


                                            <button class="btn btn-secondary btn-round" data-toggle="modal" data-target="#SMS">Send SMS</button>



                                        </div>
                                        <div class="  row row-eq-height>.alpha-box" id="search-result">
                                            @foreach (var item in Alpha)
                                            {
                                                <div class=" col-lg-4">
                                                    <div class="list-tag alpha-box p-20 m-b-30">


                                                        <h2 class="text-bold"> @item.ToUpper()</h2>
                                                        <ul class="scroll-list">

                                                            @foreach (var data in Model)
                                                            {
                                                                if (data.FirstLetter.StartsWith(item))
                                                                {

                                                                    if (ViewBag.IsFromTransactionStatement == false)
                                                                    {

                                                                        <li><a href="@Url.Action("ReciverDashboard", "Receiver", new { AgentId = data.Id })"> @data.ReceiverName </a></li>

                                                                    }
                                                                    else
                                                                    {
                                                                        <li><a href="@Url.Action("ReceiverTransactionStatement", "Receiver", new { ReceiverId = data.Id })"> @data.ReceiverName </a></li>
                                                                        <li><a href="@Url.Action("ReceiverNewTransactionStatement", "Receiver", new { ReceiverId = data.Id })"> @data.ReceiverName </a></li>

                                                                    }

                                                                }



                                                            }

                                                        </ul>
                                                    </div>
                                                </div>
                                            }
                                        </div>


                                    </div>


                                </div>
                            </div>


                        </div>
                    </div>
                </div>

            </div>
        </div>

        <div id="styleSelector">
        </div>
    </div>*@



<div class="pcoded-content">
    <div class="pcoded-inner-content">

        <div class="main-body">
            <div class="page-wrapper">

                <div class="page-body">
                    <div class="row">

                        <div class="col-sm-12">

                            <div class="card">

                                <div class="card-header border-bottom">
                                    <h4 class="f-left f-left m-t-5 m-b-0">
                                        Registered Receivers
                                    </h4>
                                    @*<a class=" btn btn-primary f-right btn-round" href="@Url.Action("RegisterAFaxer", "ViewRegisteredFaxers")">
                                            Register a Sender
                                        </a>*@
                                </div>
                                <div class="card-body">
                                    <div class="row m-t-20 m-b-20">
                                        <!--  -->
                                        <div class="col-xl-3 col-md-6">
                                            <div class="form-group">
                                                @Html.DropDownList("Country", (IEnumerable<SelectListItem>)ViewBag.Countries, "Search By Country", new { @class = "form-control form-control-round form-control-bold", id = "Country", onchange = "filter();" })
                                            </div>
                                        </div>
                                        @*<div class="col-xl-3 col-md-6">
                                                <div class="form-group">
                                                    @Html.DropDownList("City", (IEnumerable<SelectListItem>)ViewBag.Cities, "Choose City", new { @class = "form-control form-control-round form-control-bold", id = "City", onchange = "filter();" })
                                                </div>
                                            </div>*@
                                        <div class="col-xl-3 col-md-6">
                                            <div class="form-group">
                                                <input name="" class="form-control form-control-round form-control-bold" id="receiverName" placeholder="Search Receiver" onkeyup="filter();">
                                            </div>
                                        </div>
                                    </div>
                                    <div class="dt-responsive table-responsive">
                                        <table class="table table-striped table-bordered ">
                                            <thead>
                                                <tr>
                                                    <td><strong>SN.</strong></td>
                                                    <td><strong>Receiver Name</strong></td>
                                                    <td><strong>Country</strong></td>
                                                    <td><strong>Account No.</strong></td>
                                                    <td><strong>Email</strong></td>
                                                    <td><strong>Transfer Method</strong></td>
                                                    <td><strong>Action </strong></td>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                @{ int i = 0;}
                                                @foreach (var item in Model)
                                                {
                                                    ++i;
                                                    <tr>
                                                        <td>@i</td>
                                                        <td>@item.ReceiverName</td>
                                                        <td>@item.ReceiverCountry</td>
                                                        <td>@item.ReceiverAccountOrPhoneNo</td>
                                                        <td>@item.ReceiverEmail</td>
                                                        <td>@item.ServiceName</td>
                                                        <td>
                                                            @if (item.Blocked == true)
                                                            {
                                                                <a href="@Url.Action("Block", "Receiver", new { id = @item.Id, @transactionTransferMethod = item.TransactionTransferMethod  })" class="btn btn-xs btn-success waves-effect waves-light ">
                                                                    <span class="btn-label"><i class=" ti-pencil-alt"></i></span>
                                                                    UnBlock
                                                                </a>
                                                            }
                                                            else
                                                            {
                                                                <a href="@Url.Action("Block", "Receiver", new { @id = @item.Id , @transactionTransferMethod = item.TransactionTransferMethod })" class="btn btn-xs btn-danger waves-effect waves-light ">
                                                                    <span class="btn-label"><i class=" ti-pencil-alt"></i></span>
                                                                    Block
                                                                </a>
                                                            }

                                                        </td>
                                                    </tr>
                                                }

                                            </tbody>

                                        </table>

                                        Page @(Model.PageCount < Model.PageNumber ? 0 :
                                        Model.PageNumber) of @Model.PageCount

                                        @Html.PagedListPager(Model, page => Url.Action("Index",
                                          new { @page = page }))
                                    </div>

                                </div>
                            </div>

                        </div>
                    </div>
                </div>

            </div>
        </div>

    </div>
</div>


@section Scripts{
    <script type="text/javascript">


        function filter() {

            var Country = $("#Country").val();
            var City = $("#City").val();
            var receiverName = $("#receiverName").val();

            window.location.href = "/Admin/Receiver/Index?Country=" + Country + "&City=" + City + "&receiverName=" + receiverName ;

        }

        $("#receiverName").val('@ViewBag.ReceiverName');

    </script>
}

