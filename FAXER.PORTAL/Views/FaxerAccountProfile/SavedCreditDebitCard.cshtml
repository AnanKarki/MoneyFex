@model List<FAXER.PORTAL.Models.SenderSavedCreditDebitCardViewModel>
@{
    ViewBag.Title = "SavedCreditDebitCard";
    // Layout = "~/Views/Shared/_DashBoardLayout.cshtml";
    Layout = "~/Views/Shared/_New_Dashboard_Layout.cshtml";

}


@*<section class="dashboard">
        <div class="container">
            <div class="row">
                <div class="col-lg-3  hidden-xs hidden-sm col-md-4">
                    <a href="dashboard.php" class="btn btn-primary btn-sm btn-block">My Dashboard</a>
                    <br>
                    <div class="clearfix"></div>
                    @{Html.RenderPartial("_Dashboard_Sidebar");}

                </div>
                <div class="col-lg-10 col-lg-offset-1">
                    <!-- <div class="dashboard_header">
                                <h1>Estimate Faxing Fees</h1>
                    </div> -->
                    <!--  -->
                    <div class="dashboard_body">
                        <div class="  ">
                            <h3>Saved Credit/Debit Card</h3>
                            <hr>
                            <div class="table-responsive">
                                <table class="table table-bordered">
                                    <thead>
                                        <tr>
                                            <th>No.</th>
                                            <th>Card No.</th>
                                            <th>End Date</th>
                                            <th>Security Code</th>
                                            <th colspan="2">Action</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @{
                                            int index = 0;
                                        }
                                        @foreach (var item in Model)
                                        {
                                            index++;
                                            <tr>
                                                <td>@index</td>
                                                <td>@item.CardNo</td>
                                                <td>@item.EndDate</td>
                                                <td>@item.SecurityCode</td>
                                                <td><a href="" class="btn btn-default btn-sm" data-toggle="modal" data-target="#credit_debit_card_update" onclick="GetData('@item.EndMonth','@item.EndYear','@item.CardNo',@item.Id,'@item.SecurityCodeUnmask');">Update</a> </td>
                                                <td><a href="" class="btn btn-danger btn-sm " data-toggle="modal" data-target="#credit_debit_card_deletion" onclick="GetId(@item.Id);">Delete</a></td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            </div>
                        </div>
                        <a href="@FAXER.PORTAL.Common.FaxerSession.BackButtonURL" class="btn btn-sm btn-danger pull-left"><i class="fa fa-chevron-left" aria-hidden="true"></i>  Back </a>
                        @if (Model.Count == 1)
                        {
                            <button href="@Url.Action("AddADebitCreditCard","FaxerAccountProfile")" class="btn btn-sm btn-primary  pull-right" disabled> Add a Credit/Credit Card <i class="fa fa-chevron-right" aria-hidden="true"></i></button>
                        }
                        else
                        {
                            <a href="@Url.Action("AddADebitCreditCard","FaxerAccountProfile")" class="btn btn-sm btn-primary  pull-right"> Add a Credit/Credit Card <i class="fa fa-chevron-right" aria-hidden="true"></i></a>
                        }

                        <div class="clearfix"></div>
                    </div>
                    <!--  -->
                </div>
            </div>
        </div>
        <!--  -->
    </section>

    <!--  saved-credit-debit-cards.php-->
    @using (Html.BeginForm("DeleteDebitCreditCard", "FaxerAccountProfile", FormMethod.Post))
    {
        @Html.ValidationSummary(true)
        @Html.AntiForgeryToken()
        <div class="modal" id="credit_debit_card_deletion" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header">
                        <h4>MoneyFex - Credit/Debit Card Deletion</h4>
                    </div>
                    <div class="modal-body">
                        @Html.Hidden("CardId", null, new { @id = "CardId" })
                        <div class="row">
                            <div class="col-lg-3">
                                <div class="icon_al">
                                    <i class="fa fa-exclamation-triangle" aria-hidden="true"></i>
                                </div>
                            </div>
                            <div class="col-lg-9">
                                <h4>Are you sure you want to delete this Credit/Debit Card ?</h4>
                                <p>
                                    Please not that after deletion, you will not able to continue with your auto payments and top-ups!
                                </p>
                            </div>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-primary btn-sm" data-dismiss="modal">Close</button>
                        <button type="submit" class="btn btn-danger btn-sm btn-ok">Yes Delete</button>
                    </div>
                </div>
            </div>
        </div>
    }

    @using (Html.BeginForm("UpdateDebitCreditCard", "FaxerAccountProfile", FormMethod.Post))
    {
        @Html.ValidationSummary(true)
        @Html.AntiForgeryToken()
        <div class="modal" id="credit_debit_card_update" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header">
                        <h4>MoneyFex Service - Credit/Debit Card Update</h4>
                    </div>
                    @if (TempData["ErrorMessage"] != null)
                    {
                        <label class="text-danger">@TempData["ErrorMessage"]</label>
                    }*@
@*<div class="modal-body">
                        @Html.Hidden("Id", null, new { @id = "Id" })
                        <label class="text-danger">@TempData["ErrorMessage"]</label>
                        <div class="row">
                            <div class="col-lg-12">
                                <div class="col-lg-5">
                                    <label> Card Number</label>
                                </div>
                                <div class="col-lg-6 ">
                                    <div class="form-group">
                                        <input type="text" class="form-control" name="CardNumber" value="" placeholder="Card Number" id="cardnum" />
                                    </div>
                                </div>
                                <div class="clearfix"></div>
                                <!--  -->
                                <!--  -->
                                <div class="col-lg-5">
                                    <label> End Date</label>
                                </div>
                                <div class="col-lg-3 ">
                                    <div class="form-group">
                                        <input type="text" class="form-control" name="Month" value="" placeholder="Month (MM)" id="month" maxlength="2" onblur="ValidateMonth();" />
                                        @Html.ValidationMessage("MonthError", new { @class = "text-danger", @id = "MonthError" })
                                    </div>
                                </div>
                                <div class="col-lg-3 ">
                                    <div class="form-group">
                                        <input type="text" class="form-control" name="Year" value="" placeholder="Year (YY)" id="year" maxlength="2" />
                                    </div>
                                </div>
                                <div class="clearfix"></div>
                                <div class="col-lg-5">
                                    <label> Security Code</label>
                                </div>
                                <div class="col-lg-6 ">
                                    <div class="form-group">
                                        <input type="password" class="form-control" name="SecurityCode" value="" placeholder="Security Code" id="securityCode" />
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-primary btn-sm" data-dismiss="modal">Close</button>
                        <button type="submit" class="btn btn-danger btn-sm btn-ok" id="update">Update</button>
                    </div>
                </div>
            </div>
        </div>
    }

    @section Scripts{

        <!-- jQuery -->
        <script src="//code.jquery.com/jquery-1.11.0.min.js"></script>
        <!-- BS JavaScript -->
        <script src="~/JS/bootstrap.min.js"></script>
        <script type="text/javascript">
        function GetId(val) {
            document.getElementById("CardId").value = val;
        };

        function GetData(EndMonth, EndYear, CardNo, Id, SecurityCode) {
            debugger;
            document.getElementById("Id").value = Id;
            $("#Id").val(Id);
            $('#cardnum').val(CardNo);
            $('#month').val(EndMonth);
            $('#year').val(EndYear);
            $('#securityCode').val(SecurityCode);
        };

        function ValidateMonth() {
            var val = document.getElementById("month").value;
            if (val > 12) {
                document.getElementById("update").disabled = true;
                var msg = "Invalid Month"
                document.getElementById("MonthError").innerHTML = msg;
            }
            else {
                document.getElementById("update").disabled = false;
                document.getElementById("MonthError").innerHTML = "";
            }
        }
        var updatecardError = "@TempData["ErrorMessage"]";

        if (updatecardError != "") {
            debugger;

            GetData('@Model.FirstOrDefault().EndMonth', '@Model.FirstOrDefault().EndYear', '@Model.FirstOrDefault().CardNo',@Model.FirstOrDefault().Id, '@Model.FirstOrDefault().SecurityCodeUnmask');

            $("#credit_debit_card_update").modal("show");

        } else {

            $("#credit_debit_card_update").modal("hide");
        }
        </script>
    }*@



<!--  -->
<div class="real_form pattern2 pdt40 ">
    <div class="container">

        <div class="row">

            <div class="col-lg-6 col-lg-offset-3 col-md-6 col-md-offset-3 ">

         

                <div class="login_signup mb50 clearfix text-center">
                    <h3>Saved Credit/Debit Card</h3>
                    <h3 id="ErrorMsg" style="display:none" class="text-danger"></h3>
                    <hr>

                    <div class="table-responsive">

                        <table class="table table-bordered">
                            <thead>
                                <tr>
                                    <th>Card No. & Exp. Date</th>
                                    <th>Status</th>
                                    <th>Action</th>
                                </tr>
                            </thead>
                            <tbody>
                                @{
                                    int index = 0;
                                }
                                @foreach (var item in Model)
                                {
                                    index++;
                                    <tr>
                                        <td>@item.CardNo <br />@item.EndDate</td>
                                        <td>@item.Status</td>
                                        <td>
                                            @*<a href="AddADebitCreditCard" class="btn btn-primary radius-20 btn-sm btn-block" data-toggle="modal" data-target="#credit_debit_card_update" onclick="GetData('@item.EndMonth','@item.EndYear','@item.CardNo',@item.Id,'@item.SecurityCodeUnmask');">Update</a>*@

                                            <a class="btn btn-primary radius-20 btn-sm btn-block" onclick="UpdateCard(@item.Id)">Update</a>
                                           @* <a href="" class="btn btn-primary radius-20 btn-sm btn-block" data-toggle="modal" onclick="GetId(@item.Id);">Update</a>*@

                                            <a href="" class="btn btn-danger radius-20 btn-sm btn-block" data-toggle="modal" data-target="#credit_debit_card_deletion" onclick="GetId(@item.Id);">Remove</a>
                                        </td>
                                    </tr>
                                }
                            </tbody>

                        </table>
                        <hr>
                        <a onclick="Addcard()"><i class="fa fa-plus"></i> Add New Card</a>
                    </div>


                </div>
                <div class="clearfix"></div>


                <div class="text-center   clearfix">
                    <hr>

                    <!-- back btn -->
                    <div class="clearfix ">

                        <center>
                            <a href="@Url.Action("Index","SenderDashBoard")" class=" "><i class="fa fa-chevron-left" aria-hidden="true"></i>  Back to Dashboard </a>
                        </center>
                    </div>
                    <!-- back btn end-->
                </div>
            </div>

            <!--  -->

        </div>

    </div>
</div>


<!-- Delete Confirmation -->

@using (Html.BeginForm("DeleteDebitCreditCard", "FaxerAccountProfile", FormMethod.Post))
{
    @Html.ValidationSummary(true)
    @Html.AntiForgeryToken()
    <div id="credit_debit_card_deletion" class="modal fade" role="dialog">
        <div class="modal-dialog modal-md text-center">
            <div class="modal-content">
                <div class="modal-header">
                    @Html.Hidden("CardId", null, new { @id = "CardId" })

                    <button type="button" class="close" data-dismiss="modal">&times;</button>
                    <h4 class="modal-title">  Confirmation</h4>
                </div>
                <div class="modal-body uk-text-center ">

                    <h4 class="mt40"> Do you want to delete this card?</h4>
                    <hr>
                    <button type="submit" class="btn btn-danger radius-20 ">Yes</button>&nbsp;&nbsp;&nbsp;
                    @*<button type="submit" class="btn btn-danger btn-sm btn-ok">Yes Delete</button>*@
                    <button type="button" class="btn btn-default   radius-20" data-dismiss="modal">No</button>
                </div>

            </div>

        </div>
    </div>
}
          
<!-- Delete Confirmation end -->


@section Scripts{

    <script type="text/javascript">


        function Addcard() {

            var count = '@ViewBag.CardCount';
            if (count < 1) {

                window.location.href = "/FaxerAccountProfile/AddADebitCreditCard"
            } else {

                $("#ErrorMsg").text("You already have a saved card");
                $("#ErrorMsg").show();
            }
        }
        function UpdateCard(id) {
            debugger;
            window.location.href = "/FaxerAccountProfile/UpdateDebitCreditCard?CardId=" + id;
        };
        function GetId(val) {
            document.getElementById("CardId").value = val;
        };

        function GetData(EndMonth, EndYear, CardNo, Id, SecurityCode) {
            debugger;
            document.getElementById("Id").value = Id;
            $("#Id").val(Id);
            $('#cardnum').val(CardNo);
            $('#month').val(EndMonth);
            $('#year').val(EndYear);
            $('#securityCode').val(SecurityCode);
        };

        function ValidateMonth() {
            var val = document.getElementById("month").value;
            if (val > 12) {
                document.getElementById("update").disabled = true;
                var msg = "Invalid Month"
                document.getElementById("MonthError").innerHTML = msg;
            }
            else {
                document.getElementById("update").disabled = false;
                document.getElementById("MonthError").innerHTML = "";
            }
        }
        var updatecardError = "@TempData["ErrorMessage"]";

        if (updatecardError != "") {
            debugger;

            @*GetData('@Model.FirstOrDefault().EndMonth', '@Model.FirstOrDefault().EndYear', '@Model.FirstOrDefault().CardNo',@Model.FirstOrDefault().Id, '@Model.FirstOrDefault().SecurityCodeUnmask');*@

            $("#credit_debit_card_update").modal("show");

        } else {

            $("#credit_debit_card_update").modal("hide");
        }
    </script>
}
