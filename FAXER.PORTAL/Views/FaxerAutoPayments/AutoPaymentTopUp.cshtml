@model  FAXER.PORTAL.Models.AutoPaymentTopUpViewModel
@{
    ViewBag.Title = "AutoPaymentTopUp";
    Layout = "~/Views/Shared/_DashBoardLayout.cshtml";
}
@*@using (Html.BeginForm())*@
@using (Html.BeginForm("AddAutoPaymentTopUp", "FaxerAutoPayments", FormMethod.Post))
{
    @Html.ValidationSummary(true)
    @Html.AntiForgeryToken()
    <section class="dashboard">
        <div class="container">
            <div class="row">
                @*<div class="col-lg-3  hidden-xs hidden-sm col-md-4">
                    <a href="dashboard.php" class="btn btn-primary btn-sm btn-block">My Dashboard</a>
                    <br>
                    <div class="clearfix"></div>
                    @{Html.RenderPartial("_Dashboard_Sidebar");}
                </div>*@
                <div class="col-lg-9 col-lg-offset-2">
                    <!--  -->
                    <div class="dashboard_body">
                        <h3>My Auto Receiver Virtual Account Deposit</h3>
                        <hr>
                        <div class="col-lg-4">
                            <div class="form-group">
                                @Html.EditorFor(model => model.TopUpAmount, new { htmlAttributes = new { @class = "form-control", placeholder = "Enter Auto Top-Up Amount", type = "number" } })
                                @Html.ValidationMessage("Error", new { @class = "text-danger" })
                            </div>
                        </div>
                        <div class="clearfix"></div>
                        <div class="col-lg-12">
                            <p>
                                This MoneyFex Receiver Virtual Account will be credited as soon as the balance in the account reaches zero (0). Additionally, the exchange rate and fees the time will be applied.
                            </p>
                            <div class="clearfix"></div>
                            <a href="@Url.Action("MoneyFaxCardAutoTopUp")" class="btn btn-sm btn-danger pull-left">Back</a>
                            <button type="submit" class="btn btn-sm btn-primary pull-right">Proceed to set up</button>
                        </div>
                        <div class="clearfix"></div>
                    </div>
                    <!--  -->
                </div>
            </div>
            <!--  -->
        </div>
        <!--  -->
    </section>
}
